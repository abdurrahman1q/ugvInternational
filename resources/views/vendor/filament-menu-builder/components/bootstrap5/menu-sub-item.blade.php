<li>
    <a
        target="{{ $item->target }}" style="font-size: 13px"
        class="dropdown-item {{ $item->link_class }}"
        href="{{ $item->link }}"
    >
        {{ $item->name }}
    </a>
</li>
